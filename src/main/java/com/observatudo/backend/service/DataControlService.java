package com.observatudo.backend.service;

//import org.springframework.beans.factory.annotation.Autowired;

public class DataControlService {
/* 
    @Autowired
    private DataControlRepository dataControlRepository;

    public boolean isDataLoaded(String dataType) {
        return dataControlRepository.findByDataType(dataType).isPresent();
    }

    public void markDataAsLoaded(String dataType) {
        DataControl control = new DataControl(dataType, LocalDate.now());
        dataControlRepository.save(control);
    }
*/
}
